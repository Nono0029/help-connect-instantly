# Hoist @tanstack/* so Rollup can resolve internal packages (e.g. @tanstack/query-core)
# during Vercel builds that use pnpm strict isolation.
public-hoist-pattern[]=*@tanstack*
public-hoist-pattern[]=*framer-motion*
